import "./App.css";

export function App() {
  return (
    <main className="app-shell">
      <section className="app-panel" aria-labelledby="app-title">
        <p className="app-kicker">Finnplay Test Task</p>
        <h1 id="app-title">Game Catalog</h1>
        <p className="app-copy">React client scaffold is ready for the login and catalog flows.</p>
      </section>
    </main>
  );
}

export default App;
